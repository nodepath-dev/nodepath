import 'package:flutter/material.dart';

class Window {
  static double width = 0;
  static double height = 0;
}

class Pallet {

  static Color theme = Colors.black; // Primary blue
  static Color background = Color(0xFFFAFAFA); // Light gray background
  static Color insideFont = Color(0xFF1F2937); // Dark gray text

  static Color font1 = Color(0xFF6B7280); // Medium gray for secondary text
  // static Color font1 = Colors.red;
  static Color font2 = Color(0xFF1F2937); // Dark gray for primary text
  static Color font3 = Color(0xFF9CA3AF); // Light gray for tertiary text

  static Color inside1 = Color(0xFFFFFFFF); // Pure white for cards/containers
  static Color inside2 = Color(0xFFF3F4F6); // Light gray for secondary containers
  static Color inside3 = Color(0xFFE5E7EB); // Border/divider color
}
